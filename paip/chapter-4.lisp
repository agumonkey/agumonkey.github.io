


;;;;;;;;;;;;;;;; Chapter 4


(defpackage :paip-4
  (:use common-lisp))

(in-package :paip-4)

;; previously in paip

;; (defun gen (g -r)
;;   (let* ((r (g-get -r g))
;; 	 (d (g-rhs r))
;; 	 (m (lambda (o)
;; 	      (cond ((nt? o) (gen g d))
;; 		    (t (one-of d))))))
;;     (apply #'append (mapcar m d))))



;;; code begins here.

;;; end of :paip-4
